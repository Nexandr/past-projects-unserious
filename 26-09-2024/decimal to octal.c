#include <stdio.h>
int main()
{
    int num, binary[32], rem, len=0, display;
    printf("Enter a number:\n");
    scanf("%d", &num);


    printf("\nOctal equivalent of %d is %o.\n", num, num);
}
